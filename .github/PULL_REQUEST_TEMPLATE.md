## Summary

<!-- Brief description of the change -->

## Checklist

- [ ] `cargo fmt --all -- --check` passes
- [ ] `cargo clippy --workspace --exclude geo-wasm` passes
- [ ] `cargo test --workspace --exclude geo-wasm` passes
- [ ] `cargo test -p geo-wasm` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm vitest run` passes

## Breaking Changes

<!-- Any breaking changes? Document them here. -->
None.
